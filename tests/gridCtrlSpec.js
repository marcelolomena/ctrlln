/*jslint node: true */
/*global angular, describe, it, jasmine, expect, beforeEach */
"use strict";

describe("controlador de grillas", function () {
  it('algo que funcione', function() {
    var users = ['nico', 'carlos', 'cris'];
    expect(users).toEqual(['nico', 'carlos', 'cris']);
  });
});